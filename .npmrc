# Do not set `store-dir` here, and do not export PNPM_STORE_PATH to a path
# inside this repository — that materializes a huge `.pnpm-store` under the
# project root. Use pnpm's default global store (or a path outside the repo).

public-hoist-pattern[]=pdfjs-dist
